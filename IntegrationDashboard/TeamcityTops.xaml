<Page
    x:Class="IntegrationDashboard.TeamcityTops"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IntegrationDashboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="TopBreakersView" HorizontalAlignment="Left" Height="942.334" Margin="86.888,91,0,0" VerticalAlignment="Top" Width="344" RenderTransformOrigin="-0.238,-0.228" UseLayoutRounding="False" d:LayoutRounding="Auto">
            <ListView.RenderTransform>
                <CompositeTransform SkewX="-0.317" TranslateX="0.301"/>
            </ListView.RenderTransform>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="{Binding Key.Photo}" Width="128px" Height="128px" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Key.Name}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Value}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
